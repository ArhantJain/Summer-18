#include <bits/stdc++.h>
using namespace std;

int main()
{
	vector <int> v; // Declare a vector of with name v
	vector <int> v(10); // Declare an vector with size equals to 10 and elements initialised with 0
	
	vector <int> v[10]; // Declare an array of 10 vectors (initially empty)

	// Some inbuilt

	// v.size() , v.empty() , v.push_back(i) , v.resize(23) , 


	vector <string> names(20, "Raja"); // vector with some predefined initialisation( 20 is size , Raja is initialisation)

	vector< vector<int> > Matrix; // 2-D array

	vector< vector<int> >Mat(N,vector<int>(M,-1); // Vector of size NxM with -1 initialisation
	

	int ;


